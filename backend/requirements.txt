fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database (async runtime + sync driver untuk Alembic)
sqlalchemy==2.0.36
greenlet==3.1.1
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Auth (JWT + password hashing)
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pin: passlib 1.7.4 tidak kompatibel dengan bcrypt>=4.1 (breaking change deteksi backend)

# Scheduler untuk automasi pipeline ML (klasifikasi + prediksi berkala)
apscheduler==3.10.4

# Push notification (Firebase Cloud Messaging)
firebase-admin==7.5.0

# Utility
python-dotenv==1.0.1
